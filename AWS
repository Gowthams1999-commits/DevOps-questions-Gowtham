• How do you set up a VPC in AWS?
dckr_pat_GRhm6AOJ9KYjkwJZIURl7UqB-3c
• How do you assign internet access to an EC2 instance?

• What is a NAT Gateway in AWS?

• Why should we choose EKS over ECS, even though ECS supports Fargate? (Capgemini asked this)

• How do you set up an AWS Lambda function? (Asked by a software company)

• How do you create and configure an EKS cluster? (Asked by a software company)

• If the .pem key for an EC2 instance is lost, can it be restored? What are the alternatives? (Asked by a software company)

• What are Lambda Layers in AWS? How are they used?

* Have you performed any cloud migration from on-premises to any cloud?

Comcast : (Importent)

* Can you explain a highly available architecture in AWS?

* How do you create and configure an EKS cluster in AWS?
* what is differnece between public ip address and private ip address in vpc?
* how many vpc can we create in per region.

* What is AWS Lambda and how does it work?

* If I lose my .pem key for an EC2 instance, is there a way to recover access?

* What is cross account access 
* what is difference between ALB and NLB --> LTI mindtree
* what is Gateway LB --> LTI mindtree
* i want to deploy monolith app in AWS which service i will choose Ec2 or EKS or onpremises vm --> LTI mindtree

* what is Assume Role in AWS

Anovia Company:
1. Can you explain about API in aws
2. what is difference between RESET API and RESETFUL API
3. What is difference between public and private subnet


* How to access Acount B resources in Account A

* Cost Optimization in aws

# ☁️ AWS

## General

* Have you performed any migrations from on-premises to cloud? What challenges did you face?
* Explain the cloud architecture you have worked on.
* You created an IAM user in AWS and configured role-based access in EKS. How do you bind the IAM user to the EKS role?
* Assume you have 10 AWS accounts. How will you securely log in to them without using access keys?
* What are the different ways to log in to an AWS account?
* Does Amazon S3 require a VPC?
* If secrets are created in AWS Secrets Manager, how can Amazon EKS access those secrets?
* How do you configure RBAC in Amazon EKS?

## Action Labs

* How do you detect the root cause when an application goes down in the cloud?
* How do you configure a VPC for high availability?

## Akkami

* IPv4 vs IPv6.
* What range does an IPv4 address start from?

## Alphadyne

* An application is hosted on a public EC2 instance. How would you migrate it to a private subnet following AWS best practices for security, networking and high availability?
* How do you provide HTTPS access to an application hosted in a private subnet?
* Explain ALB vs NLB.
* How do ACM certificates work?
* Explain routing and security groups in this architecture.

## Altimetrik

* What is your understanding of software architecture components such as:

  * Load balancers
  * Web servers
  * Application servers
  * Databases
  * Integrations

## Amazon

* How do you send log files from EC2 to S3?
* Explain three-tier architecture.
* An admin does not have access to an S3 bucket. How can an IAM permission boundary cause this?
* You have an S3 bucket in one AWS region. Can it be accessed from another AWS region?
* Is it possible to create a NAT Gateway in a private subnet?
* How do you configure Cluster Autoscaler?


